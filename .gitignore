# קבצי המקור הכבדים נשארים מקומית — כל מה שנגזר מהם כבר בתוך site/
# (שני הסרטונים המקוריים וכל תמונות ה-PNG ברזולוציה מלאה — כ-81MB)
/*.png
/*.mp4
/*.jpg
/*.jpeg

# מערכת
Thumbs.db
desktop.ini
.DS_Store

# תלויות
node_modules/

# חבילת נכסים שנזנחה — הכל מוגש היום מ-site/
/assets-repo
